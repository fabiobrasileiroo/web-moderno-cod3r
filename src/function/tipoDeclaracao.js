console.log(soma(3, 2));
// function declaration
function soma(x, y) {
  return x + y;
}
// function expression
const sub = function (x, y) {
  return x - y;
};
console.log(sub(3, 4));
// named function expression
// so fica bom para debugar
const mult = function mult(x, y) {
  return x * y;
  
};
